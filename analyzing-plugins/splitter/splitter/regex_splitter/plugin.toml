plugin_id = "regex_split"
name = "正则切分器"
version = "0.1.0"
plugin_role = "biz"
plugin_type = "splitter"
runtime_mode = "inproc"
sdk_version = ">=1.0.0"
summary = "使用正则表达式切分文本。"
description = "一个支持自定义正则分隔模式的进程内切分插件。"
form_schema_file = "form.schema.json"

[capabilities]
accepts_text = true
accepts_structured_input = false
returns_multiple = true
returns_hierarchical = false

[runtime]
kind = "inproc"
search_paths = [""]

[runtime.entrypoint]
module = "regex_split.plugin"
attribute = "build_plugin"
