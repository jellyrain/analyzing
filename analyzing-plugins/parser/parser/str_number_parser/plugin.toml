plugin_id = "str_number"
name = "字符串数字解析器"
version = "0.1.1"
plugin_role = "biz"
plugin_type = "parser"
runtime_mode = "inproc"
sdk_version = ">=1.0.0"
summary = "直接从文本中提取数字内容。"
description = "一个面向数字文本抽取的进程内解析插件。"
form_schema_file = "form.schema.json"

[capabilities]
accepts_text = true
accepts_structured_input = false
returns_multiple = true
returns_hierarchical = false

[runtime]
kind = "inproc"
search_paths = [""]

[runtime.entrypoint]
module = "str_number.plugin"
attribute = "build_plugin"
